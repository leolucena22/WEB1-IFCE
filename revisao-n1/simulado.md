Perfeito — sua prova mistura **HTML, CSS e JavaScript básico**, com foco em conceitos e prática. Vou montar um simulado bem parecido com o estilo da sua prova 👇

---

# 🧠 SIMULADO – WEB 1 (HTML, CSS e JavaScript)

## **1. (CSS - Box Model)**

Se um elemento possui:

* `width: 150px`
* `padding: 10px`
* `border: 5px`

Qual será a largura total ocupada?

a) 150px
b) 170px
c) 180px
d) 190px

---

## **2. (JavaScript - Arrays)**

Qual método é utilizado para **adicionar um elemento no final de um array**?

a) shift()
b) pop()
c) push()
d) unshift()

---

## **3. (DOM)**

Qual método seleciona um elemento pelo **id**?

a) document.querySelectorAll()
b) document.getElementById()
c) document.getElementsByClassName()
d) document.querySelectorAll("#id")

---

## **4. (HTML - Semântica)**

Qual tag representa o **título mais importante da página**?

a) `<title>`
b) `<h6>`
c) `<h1>`
d) `<header>`

---

## **5. (JavaScript - JSON)**

Qual método converte um objeto JavaScript em JSON?

a) JSON.parse()
b) JSON.stringify()
c) Object.toJSON()
d) JSON.convert()

---

## **6. (JavaScript - Eventos)**

Escreva um código que adicione um evento de clique em um botão com id `"btn"` e mostre um alert com a mensagem "Clicado".

---

## **7. (JavaScript - Comparação)**

Qual a diferença entre:

```javascript
== 
===
```

Dê um exemplo prático.

---

## **8. (HTML - Acessibilidade)**

Para que serve o atributo `alt` em imagens?

---

## **9. (Web Storage)**

Qual a diferença entre:

* localStorage
* sessionStorage

---

## **10. (JavaScript - map)**

Transforme o array:

```javascript
[2, 4, 6]
```

em um novo array com o triplo de cada valor usando `.map()`.

---

## **11. (CSS - Display)**

Qual a diferença entre:

* `display: block`
* `display: inline`
* `display: inline-block`

---

## **12. (CSS - Flexbox)**

Qual propriedade alinha os itens horizontalmente no flexbox?

a) align-items
b) justify-content
c) flex-direction
d) align-content

---

## **13. (JavaScript - Funções)**

Qual a diferença entre:

```javascript
function soma() {}
```

e

```javascript
const soma = () => {}
```

---

## **14. (HTML - Formulários)**

Qual atributo é usado para tornar um campo obrigatório?

a) required
b) validate
c) needed
d) must

---

## **15. (CSS - Responsividade)**

O que faz a media query abaixo?

```css
@media (max-width: 600px) {
  body {
    background: red;
  }
}
```

---

# 🔥 Se quiser subir o nível:

Posso fazer:

* Simulado com **respostas comentadas**
* Prova estilo **pegadinha de professor**
* Ou até um **resumo completo + cola de prova 😏**

Só me fala 👍
